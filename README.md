# Current Editor

Current Editor 是一个轻量级、现代化的代码编辑器，旨在提供简洁高效的编程体验。它支持多种编程语言的语法高亮，并具备丰富的代码编辑功能。

## 🌟 特性

- **多语言语法高亮**：支持 Python、C/C++、Java、JavaScript、HTML、CSS、Ruby、Rust、Go、Objective-C、Bash 等数十种编程语言。
- **多文件编辑器**：采用标签页式管理，支持同时编辑多个文件，轻松切换。
- **自动保存机制**：每 5 秒自动保存当前编辑内容，有效防止数据丢失。
- **智能语法分析**：基于 AST 语法树的高级代码高亮，精确区分变量、函数名、包名等不同数据类型。
- **可扩展主题系统**：内置多种深色/浅色主题（如 VS Code、GitHub、Dracula 等），支持自定义主题配置。
- **多语言界面**：支持中文、英文、法文、德文、日文、俄文等多种语言界面，满足全球用户需求。
- **集成 AI 功能**：接入 Deepseek-R1 大模型，提供智能代码建议、错误修复等辅助功能。
- **命令行集成**：内置命令行界面，支持直接运行代码和系统命令。
- **完整日志系统**：详细的日志记录功能，便于调试和问题排查。
- **首次启动优化**：自动检测并下载 Python 环境，智能选择最优安装源。

## 🚀 安装

### 前置条件

- Python 3.x 环境

### 安装步骤

1. 克隆本仓库到本地：

   ```bash
   git clone https://gitee.com/chengzi404-byte/phoenix-editor.git
   ```

2. 进入项目目录：

   ```bash
   cd phoenix-editor
   ```

3. 运行主程序：

   ```bash
   python app.py
   ```

## 💡 使用方法

### 基本操作

- **新建文件/窗口**：点击菜单栏 `文件 > 新建文件 / 新建窗口`
- **打开文件**：点击菜单栏 `文件 > 打开文件`
- **保存文件**：点击菜单栏 `文件 > 保存文件`
- **运行代码**：点击菜单栏 `运行 > 运行` 或使用快捷键 `F5`
- **查看帮助**：点击菜单栏 `帮助` 查看版本信息和使用指南

### 设置

点击菜单栏 `设置 > 打开设置面板` 可调整以下参数：

- 字体与字号
- 文件编码格式
- 界面语言
- 主题样式
- 自动保存间隔

## 📁 项目结构

```
current-editor/
├── asset/              # 资源文件目录
│   ├── icons/         # 图标资源
│   ├── packages/      # 包资源
│   ├── theme/         # 主题文件
│   └── settings.json  # 配置文件
├── lang/              # 多语言支持
├── library/           # 核心库模块
│   ├── highlighter/   # 语法高亮实现
│   ├── api.py         # 核心接口
│   ├── editor_operations.py  # 编辑器操作
│   ├── logger.py      # 日志系统
│   ├── multi_file_editor.py  # 多文件编辑器
│   ├── py_executable_check.py  # Python环境检查
│   └── startup.py     # 首次启动操作
├── ui/                # UI界面模块
│   ├── main_window.py # 主窗口
│   ├── menu.py        # 菜单
│   ├── tabs.py        # 标签页组件
│   └── file_browser.py  # 文件浏览器
├── test/              # 测试代码
├── app.py             # 主程序入口
└── README.md          # 项目说明
```

## 🛠️ 核心模块

- **library/highlighter/**：各种编程语言的语法高亮实现，支持精确的代码着色。
- **library/multi_file_editor.py**：多文件编辑功能，支持标签页管理和文件切换。
- **library/editor_operations.py**：核心编辑操作，包括复制、粘贴、撤销、重做等功能。
- **library/logger.py**：完整的日志记录系统，便于调试和问题追踪。
- **library/startup.py**：首次启动时的自动配置和环境检查。
- **ui/main_window.py**：应用程序主窗口，负责界面布局和组件管理。
- **ui/tabs.py**：标签页组件，包括设置面板和帮助面板。

## 🧪 测试

测试代码存放在 `test/` 目录下，使用 Python 标准库的 unittest 框架编写：

```bash
python -m unittest discover test
```

## 📝 版本历史

- **v0.1.1**：优化语法高亮逻辑，改进用户界面
- **v0.1.0**：初始版本，包含基本编辑和运行功能

## 🤝 贡献

欢迎提交 Issue 和 Pull Request 来帮助改进项目！

### 贡献流程

1. Fork 本仓库
2. 创建新分支 (`git checkout -b feature/new-feature`)
3. 提交更改 (`git commit -m 'Add new feature'`)
4. 推送分支 (`git push origin feature/new-feature`)
5. 创建 Pull Request

## 📄 许可证

本项目采用 Apache 2.0 License 开源协议，详情请参阅 [LICENSE](LICENSE) 文件。

## 📞 联系我们

- Gitee 项目主页：https://gitee.com/chengzi404-byte/phoenix-editor
- Issue 跟踪：https://gitee.com/chengzi404-byte/phoenix-editor/issues

---

**Current Editor** - 让编程更高效！